//constant conversion variable
const conversion = 1.60934;

//miles variable
let miles = 20;

//prints the conversion calculation
console.log("The distance of " + (miles * conversion) + " kms is equal to " + miles + " miles");
